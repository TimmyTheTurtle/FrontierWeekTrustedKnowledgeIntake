[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "contextual-knowledge"
version = "0.0.0"
description = "Foundational models for Trusted Knowledge Intake"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

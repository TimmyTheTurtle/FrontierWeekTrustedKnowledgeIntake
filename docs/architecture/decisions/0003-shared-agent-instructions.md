# ADR 0003: Shared agent instruction model

## Status
Accepted

## Decision
Use root `AGENTS.md` as canonical instructions and keep `.github/copilot-instructions.md` as a minimal adapter.

## Consequences
- Standing rules are updated once in one canonical file.
- Vendor-specific instruction drift is reduced.
